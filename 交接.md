# ChillDesktop 交接文档

> 最后更新：2026-09-10
> 状态：**核心体验已验证通过（9/9），但尚未完美——还有 1 个已知问题：Win+D 会显示空桌面（详见 2.6 / 5.1）。**
> **产品形态已定调（见 1.5）：最终交付物不是外部启动器，而是「拖进游戏目录即生效」的 BepInEx 插件。现有启动器是技术验证原型，其 Win32 逻辑 ~90% 可直接移植。** 下一步：插件化 → 修 Win+D → 打包 → GitHub。

---

## 1. 这个项目是什么

把 Steam Unity 游戏 **《Chill with You Lo-Fi Story》** 变成一个**可交互的动态桌面壁纸**：

- 游戏全屏铺在桌面背景层，女孩持续动画、lo-fi BGM 持续播放；
- IDE / 浏览器 / 资源管理器等办公窗口正常浮在她上面，缝隙处能看到她；
- 游戏内原生 UI（笔记 / 待办 / 习惯 / 日历 / 隐藏UI）**在壁纸状态下可正常点击**；
- 游戏右侧「笔记」按钮旁边，额外伪装了一个等大的灰色圆圈按钮 **「工具箱」**，点击打开桌面上真实的 `chillgirl` 文件夹（用户自己往里面放应用快捷方式）；
- 点击壁纸层不抢办公窗口焦点、不把游戏抬到办公窗口之上、Win+D 不最小化游戏。

### 硬约束（全程不可违背）

- **不修改任何游戏文件；不重装/不使用 BepInEx；游戏保持纯净 vanilla。**
- 本程序是一个**完全外部**的 Win32 托盘小程序，只通过公开 Win32 API 操作游戏窗口。
- 游戏 exe 路径（默认）：
  `C:\Program Files (x86)\Steam\steamapps\common\Chill with You Lo-Fi Story\Chill With You.exe`
- 与用户交流用**中文**。

---

## 1.5 产品形态（2026-09-10 深夜用户拍板）：插件，而不是外部启动器

**用户原话要点**：「我想做成一个插件，所有购买了这个游戏的人，只需要拖入一个文件到游戏目录下就生效，原理代码你来打包；团队只有我一个人；技术栈你定，全托管。」

### 技术选型（我替用户定的，下次如有更强理由可改）

**BepInEx 5（x64 / Mono）插件，C#，目标框架 net46。**

- 游戏已确认是 **Unity 2022.3 Mono、x64**（不是 IL2CPP）→ BepInEx 5.4.x 稳定支持，这是 Unity Mono 单机游戏 mod 的业界标准。
- 插件运行在**游戏进程内**，但**仍然沿用本项目已验证的顶级窗口方案**（对游戏自己的 HWND 调 SetWindowLong/SetWindowPos），**不要**走 SetParent(Progman) 老路（2.2）。现有 `DesktopLayer.cs / User32.cs / FolderWidget.cs / LayeredRenderer.cs` 的 Win32 逻辑几乎原样可搬。
- 进程内还白赚三样东西：① 可合法 subclass 游戏窗口（同进程 SetWindowLongPtr GWLP_WNDPROC），Win+D 防御多了手段（见 2.6）；② 工具箱按钮未来可用 Unity IMGUI(OnGUI) 直接画在游戏画面里（位置/DPI/Z序问题全部消失，点击走 Unity 事件——顶级窗形态下点击本来就能进 Unity）；③ 不再需要单实例 mutex / 找游戏进程 / 拉起游戏 / --quit IPC 那一整套。
- BepInEx 配置（BepInEx/config/*.cfg）：chillgirl 文件夹路径、启用开关等。

### "拖入一个文件"的现实与交付形态（重要，下次要跟用户讲清楚）

严格的"单个文件"技术上只能做 DLL 劫持自举（`version.dll` 单文件引导 CLR + 托管 DLL 嵌入资源），维护成本高、杀软误报率高，**不推荐**。实际给用户的体验可以做到一样简单，二选一：

- **方案 A（推荐先做）**：发一个压缩包，里面是 BepInEx 全家桶（`winhttp.dll`(Doorstop) + `BepInEx/` 文件夹，插件 dll 已放在 `BepInEx/plugins/`），用户**整个拖进游戏根目录**即完成。装过其他 Unity mod 的用户零学习成本；README 给一张动图。
- **方案 B（更贴"一个文件"）**：做一个**单文件安装器 exe**（用户只需运行这一个文件，它自动找 Steam 库路径、部署上述文件）。可以用现有 .NET 8 工程或 Inno Setup，等插件本体完成后再做。

注意：BepInEx 是 **LGPL-2.1**，随包分发没问题（保留其 LICENSE）；**不得打包任何游戏原始文件/资源**。这是单机休闲游戏、无反作弊，mod 风险低；GitHub 上注明"非官方 mod、与开发商无关、自负风险"。Steam 校验完整性不会删第三方散装文件，游戏更新一般也不影响 BepInEx（大版本更新后需要复测）。

### 与早期"不装 BepInEx / 不改游戏"约束的关系

那条约束是**开发期在用户本机上**的保护要求（曾有嵌入 AI 的废案，用户不想再把本机游戏环境搞脏），不是产品约束。做插件版时，在用户本机安装 BepInEx 做实测是必要的一步，**先征得用户同意再装，并保留卸载方法（删掉那几个文件游戏即恢复纯净）**。

---

## 2. 开发历程与关键认知（按时间，含踩坑）

### 2.1 游戏侧事实（早期反编译/实测确认）

- Unity 2022.3 Mono，D3D11 flip-model；窗口类 `UnityWndClass`，标题 `Chill With You`。
- 游戏自带 Window / BorderlessFullScreen 两种模式，**窗口可被 SetParent / 改样式**。
- **失焦不暂停**：`RoomGameManager.OnApplicationFocus` 只切 vSync / targetFrameRate；模拟、动画、音频继续。失焦自动降到约 24fps（省电，对 lo-fi 房间足够）。
- **Unity 用全局鼠标状态（GetAsyncKeyState 一类）处理悬停**——所以即使窗口收不到鼠标消息，鼠标移到图标上人物/UI 也会有反应。这点曾经误导排查方向（"悬停有反应=输入通了"，其实点击和悬停是两条路）。

### 2.2 废案：挂进 Progman / WorkerW 当子窗口（Wallpaper Engine 那套）

最经典的壁纸做法：`SendMessage(Progman, 0x052C)` 造 WorkerW，再 `SetParent(游戏, WorkerW/Progman)` 铺满。
**在本机 Win11 24H2/25H2 (build 26200) 上此路不通**，已实测确认：

- WS_CHILD 子窗口**几何上能被命中测试（WindowFromPoint 找得到），但系统不投递真实鼠标点击消息**（GDI 测试窗和 Unity 游戏都一样）；
- 结果正是用户报告的现象：**悬停有反应、所有原生按钮点击全失效**；
- 试图用 PostMessage 伪造 WM_LBUTTONDOWN 转发也无效（Unity 不认、坐标/焦点语义也不对）。

**这不是代码 bug，是系统对子窗口输入的限制，无法绕过。** 任何"把子窗口点击转发进去"的尝试都别再走。

### 2.3 最终成立的架构（Rainmeter 桌面挂件同款）

游戏保持为**独立顶级窗口**，三件套：

```
样式   : WS_POPUP（无边框、无标题栏、不可调大小）+ WS_VISIBLE + WS_CLIPSIBLINGS
扩展样 : WS_EX_LAYERED (alpha=255) + WS_EX_TOOLWINDOW + WS_EX_NOACTIVATE
位置   : 铺满主屏 (0,0,ScreenW,ScreenH)，SetWindowPos(HWND_BOTTOM, SWP_NOACTIVATE)
```

实测三者兼得：

- **真实点击正常投递给游戏**（顶级窗口，系统正常发鼠标消息）；
- **NOACTIVATE**：点击不抢前台焦点、不把窗口抬到办公窗口之上；
- ~~NOACTIVATE 顶级窗口不会被 Win+D / 显示桌面 / MinimizeAll 最小化~~
  **【此结论在独立小实验里成立，但集成到真实游戏后不成立！见 2.6，Win+D 是当前未解决问题】**；
- TOOLWINDOW：不出现在任务栏 / Alt+Tab；LAYERED+alpha255：让 Unity D3D11 flip-model 走独立合成，铺底稳定。

桌面图标通过单独隐藏 `SHELLDLL_DefView` 处理，和游戏窗口解耦（不再需要 0x052C/WorkerW）。

### 2.4 工具箱按钮的两个坑

1. **窗口类必须自定义注册**。预定义 `"Static"` 类的 WM_NCHITTEST 默认返回 HTTRANSPARENT（点穿），顶级窗也收不到点击；自己 `RegisterClassW`（默认 HTCLIENT）才行。圆外用 WM_NCHITTEST 返回 HTTRANSPARENT 做逐形状穿透。
2. **Z 序方向写反过**：`SetWindowPos(A, B)` 的语义是把 A 放到 B **下方**。想让工具箱贴在游戏"上面"，正确做法是**两次沉底**——先把工具箱 HWND_BOTTOM，再把游戏 HWND_BOTTOM；后沉的更靠底，于是顺序为：
   `办公窗口 … 工具箱 → 游戏 → Progman`。
   心跳每 3 秒重新维持一次这个顺序。

### 2.5 DPI 陷阱（本机 2560×1600 物理 / 150% 缩放）

- 启动器是 PerMonitorV2。**跨进程对游戏窗口调用 GetClientRect/GetWindowRect 会拿到 DPI 虚拟化后的 1707×1067 系坐标**，导致按钮缩到 72px、位置错乱。
- 几何一律用 `GetSystemMetrics(SM_CXSCREEN/SM_CYSCREEN)` = 真实 2560×1600；UpdateLayeredWindow 坐标也是物理像素。
- 绘制：布局坐标 × DPI 缩放，但 **GDI+ 字号不要乘缩放**（HDC 会自动放）；透明 32bpp DIB 上文字用**灰度 AntiAlias**，ClearType 会在透明层留 RGB 彩边。

### 2.6 ⚠️ 已知未解决问题：Win+D 会显示空桌面（下次第一优先级）

用户实机报告（2026-09-10）：壁纸状态下按 **Win+D，游戏被最小化/藏走，露出空桌面**（桌面图标本来就被隐藏，所以是纯空壁纸）。
这与早期独立实验（对测试窗发 MinimizeAll 命令、检查 IsIconic）"NOACTIVATE 不被最小化"的结论矛盾——**实验条件不等于真实 Win+D 按键**，不要再相信旧结论。

**下次先做 5 分钟复现取证**（在 `poc/` 写个小脚本，按下 Win+D 后连续轮询 3 秒）：

1. `IsIconic(游戏)` 是否变真？还是窗口没最小化、只是被别的东西（Progman/WorkerW 提升、桌面合成器 peek）盖到了下面？
2. 游戏的 Z 序位置/visible 状态变化；前台窗口变成什么。
3. 启动器 3 秒心跳里 `IsIconic → EmbedGame` 的自愈**到底有没有触发、触发后能否救回来、救回延迟多少**（可能已经会自愈但慢/不彻底，用户看到的就是那几秒空桌面；也可能 Win+D 走的不是 iconic 路径，心跳根本检测不到）。
4. 再按一次 Win+D（恢复所有窗口）时游戏是什么状态，会不会出现重复/错位。

**候选修复方向（按推荐顺序，逐个实机试）：**

- **a. 快速轮询自愈（最稳妥的兜底）**：单独起一个 200~300ms 的轻量 Timer，只查 `IsIconic(GameHwnd)`（和 rect），一发现立刻 EmbedGame 还原。即使不能"免疫"，也能把空桌面时间压到 0.3 秒内，观感上等于没消失。注意恢复后要维持 工具箱→游戏 的两次沉底顺序。
- **b. 给游戏窗口设一个"永不最小化的属主"**：被属主持有的窗口不会随 MinimizeAll 独立最小化。可在启动器里建一个隐藏的 WS_EX_TOOLWINDOW+NOACTIVATE 消息窗，跨进程 `SetWindowLongPtr(游戏, GWLP_HWNDPARENT, 属主)` 试试（跨进程属主合法性要实测，Detach 时必须还原属主=NULL）。
- **c. 拦截最小化**：跨进程不能 subclass 游戏（不能注入），但可试 `SetWinEventHook(EVENT_SYSTEM_MINIMIZESTART/END, ...)` 或更可靠的 `EVENT_OBJECT_LOCATIONCHANGE` 监听游戏窗，一变 iconic 立即还原（等同 a 但事件驱动，延迟更低）。
- **d. 排查是不是 Win+D 把 Progman/WorkerW 提升到了游戏上方**：若取证显示游戏没 iconic 只是被盖，则在自愈里把游戏（和工具箱）重新沉底即可，甚至根本不用 SW_RESTORE。
- 备选思路（仅当 abc 全失败）：Win+D 时干脆短暂恢复 SHELLDLL_DefView 图标层，让用户看到正常桌面图标而不是"空桌面"——但这改变了产品形态，先问用户。

注意：**不能**回到把游戏 SetParent 进 Progman/WorkerW 的路子来换 Win+D 免疫——那条路点击不进游戏（见 2.2）。

---

## 3. 代码结构

> 现有工程是**外部启动器原型**（.NET 8 WinForms）。插件化时它的价值是"已验证的 Win32 配方库"，
> 大部分类可直接拷进 BepInEx 插件工程；会被丢弃/重写的只有 Program 生命周期、NotifyIcon 托盘、mutex/IPC。

工程目录：`C:\Users\23122\ChillWithYouAI\ChillDesktop\ChillDesktop\`
（.NET 8 WinForms，仅用 NotifyIcon 托盘 + Win32 P/Invoke）

| 文件 | 职责 |
|---|---|
| `Program.cs` | 入口：单实例 mutex、`--quit` IPC、找游戏/拉起游戏、装配、托盘菜单、3 秒心跳、退出清理 |
| `Desktop/DesktopLayer.cs` | 找游戏窗口、定位 Progman/SHELLDLL_DefView、EmbedGame（顶级沉底三件套）、Detach 还原、隐藏/恢复桌面图标、心跳自愈 |
| `Widget/FolderWidget.cs` | 工具箱圆圈：自定义窗口类、ULW 逐像素透明绘制、悬停「工具箱」气泡、圆外穿透、点击开 chillgirl、维持 Z 序。**移植注意：依赖 WinForms 的 NativeWindow，插件里要改成"RegisterClassW 时直接把托管 delegate 当 lpfnWndProc"（本文件静态构造里已有这个 delegate，去掉 NativeWindow 继承即可）；或干脆改 IMGUI 方案（见 1.5）** |
| `Widget/LayeredRenderer.cs` | UpdateLayeredWindow 呈现（top-down 32bpp 预乘 alpha DIB） |
| `Native/User32.cs` | 全部 Win32 声明 |
| `Logger.cs` | 写 `chilldesktop.log`（与 exe 同目录） |

### 运行时行为要点

- **单实例**：mutex `ChillDesktop_SingleInstance`。
- **IPC 退出**：命名事件 `Local\ChillDesktop_Quit`；再跑一次 `ChillDesktop.exe --quit` 会通知在跑的实例走优雅退出（ApplicationExit → Cleanup）。
- **附加/启动**：先找顶级游戏窗，再找 Progman 下的子窗（兼容旧状态/重连），都没有才启动游戏；等到 UnityWndClass 后再稳定 8 秒。重连不会重复启动游戏。
- **托盘菜单**：重新嵌入桌面 / 显示-隐藏桌面图标 / 退出（游戏继续运行）/ 退出并关闭游戏。
- **心跳（3s）**：游戏被重新挂走/矩形变化/被最小化 → 重新 EmbedGame；Explorer 重启 → 重新定位图标层；工具箱窗口丢失 → 重建；连续 3 次找不到游戏 → 启动器退出。
- **优雅退出（Cleanup / Detach）**：还原游戏原始样式+扩展样式+原矩形、SW_RESTORE、恢复桌面图标。可选杀游戏进程。

### PoC / 验证脚本

目录：`C:\Users\23122\ChillWithYouAI\ChillDesktop\poc\`

- `embed_wallpaper.py`：ctypes 基础设施（找窗、DPI 感知、枚举回调类型），被其他脚本 import。
- `capture_game.py`：用 `PrintWindow(PW_RENDERFULLCONTENT=2)` 抓游戏画面（不依赖前台/遮挡），存 2560×1600。
- **`verify2.py`：当前正式的端到端验收脚本，9 项检查，不清场用户窗口。**
  - 它会把恰好盖住两个测试点的窗口临时平移到屏幕外、结束精确归位（含 atexit 兜底）。
  - **注意坑**：遮挡者里可能有 `Chrome_RenderWidgetHostHWND` 这种**子窗口**，平移坐标语义与顶级窗不同；脚本处理完顶级窗归位后，Chrome 一般会自己 relayout，必要时手动把渲染子窗 MoveWindow 铺满父客户区。
  - 面板开合判据：笔记列表选中项的**品红描边像素**（夜间房间场景深色，不能用"画面明暗"判断面板，曾因此误判）。
- 历史实验脚本（可留作参考）：`diag_input.py`、`exp_toplevel.py`、`exp_raise.py`、exp_raise2.py`、`exp_noactivate.py`、`exp_na2.py`（NOACTIVATE 顶级可点且不抢焦的决定性实验）、`verify_final.py`（旧版，会清场且判据有误，已被 verify2 取代）。

---

## 4. 当前阶段（成熟度）

**核心功能完成并实机验证。** `verify2.py` 九项全过：

1. Z 序：工具箱紧贴游戏上方；2. 工具箱 hit-test 可点；3. 原生按钮区 hit-test 到 Unity；
4. 真实点击笔记 → 面板开；5. 再点 → 面板关；6. 点击游戏不抢焦点；
7. 点击后游戏不上抬；8. 点工具箱 → 新开标题含 `chillgirl` 的资源管理器；9. 全程不抢焦。

此前已验：悬停增亮 + 气泡、圆外点击穿透、不抢焦、退出还原游戏窗+图标、重连不重复启游戏、跨进程 DPI 几何正确。
**未通过**：~~Win+D 不最小化~~ —— 实机按 Win+D 仍是空桌面（见 2.6），这是发布前唯一已知功能缺口。

**构建**：`dotnet build` 0 错误（在 `ChillDesktop\ChillDesktop\` 目录，注意 csproj 在这一层）。
**当前运行实例**：独立进程（经 `cmd start` 分离启动，避免挂在工具进程树上被内存回收连带杀掉）。日志见 bin 目录下 `chilldesktop.log`。

> 本机 2026-09-10 凌晨曾因系统内存紧张，后台任务方式启动的启动器被系统回收过一次。
> 教训：**长期常驻实例用资源管理器/`start` 分离启动**，不要挂在可能被回收的父进程下。

---

## 5. 下次要做的事（插件版路线图，按优先级）

> 前提：征得用户同意后，在本机游戏装一次 BepInEx 5 x64（解压到游戏根目录，运行一次游戏生成插件目录；删掉那几个文件即可完全卸载）。

1. **新建插件工程**（建议放在仓库 `ChillDesktop.Plugin/`，与原型启动器并存）
   - 类库 `net46`，引用 BepInEx/core 下的 `BepInEx.dll`、`0Harmony.dll`；`[BepInPlugin(GUID,Name,Version)]` + `BaseUnityPlugin`。GUID 建议 `com.<user>.chilldesktop`。
   - 把原型的 `User32.cs / DesktopLayer.cs / LayeredRenderer.cs / FolderWidget.cs` 拷过去改造：去掉 WinForms 依赖，窗口过程用 RegisterClassW + 静态 delegate（FolderWidget 静态构造里已有现成 delegate）。
   - 入口：`Awake()` 协程等待游戏自己的 HWND（`Process.GetCurrentProcess().MainWindowHandle`），执行现有 EmbedGame 配方；`OnDestroy()` 执行现有 Detach 还原。不需要 mutex / 找游戏 / 拉起 exe / --quit IPC。
2. **【第一功能缺口】修 Win+D 空桌面**（取证和候选方案见 2.6）。插件在游戏进程内，多两个手段：① 同进程 subclass（GWLP_WNDPROC）拦截最小化/样式变更；② Unity 协程每帧或 0.2s 自检恢复，延迟可低到无感。修好前不发布。
3. **工具箱插件化**：先 1:1 移植 Win32 外挂圆圈（验证最快）；再试 **IMGUI 版**（OnGUI 画圆，点击走 Unity 事件，天然对齐游戏 UI、无 DPI/Z序问题），观感达标就作正式版，外挂窗留作 fallback。打开 chillgirl 用 `Process.Start`。
4. **打包发布**：BepInEx 全家桶 + 插件 dll 一个压缩包，拖进游戏根目录即生效（方案 A）；后续可选单文件安装器 exe（自动找 Steam 库，方案 B）。README 写清安装/卸载三步。
5. **GitHub 上线**：中文 README（效果图/GIF：壁纸效果、原生 UI 可点、工具箱、Win+D 表现）；自己代码的 LICENSE + 随附 BepInEx 的 LGPL 说明；**非官方 mod 免责声明、不分发任何游戏文件**；`.gitignore`（bin/obj/*.log）。原型启动器工程保留在仓库作参考或归档分支。
6. 可选增强（插件版同样适用）：多显示器策略；分辨率/DPI 变化实时重排；插件里可直接调 `Application.targetFrameRate` 解决失焦 24fps 观感；按钮位置常量（NoteCx=2454, NoteCy=667, NoteR=51, ToolDx=-132）做成 cfg；工具箱图标美术升级（当前 GDI+ 手绘，ring 约 RGB(153,153,190)）。

### 测试现状（用户确认：一人团队、无既有自动化测试）

目前唯一自动化验证是 `poc/verify2.py`（9 项 E2E：PrintWindow 抓帧 + 模拟点击 + 窗口枚举）。插件化后它仍可当黑盒冒烟测试；另需补：干净的游戏+BepInEx 环境安装/卸载测试、游戏大版本更新后兼容性复测。

---

## 6. 一句话给下一个我

> 最终产品是 **BepInEx 5 插件**（拖进游戏目录即生效），不是外部启动器；但窗口配方不变：**顶级 WS_POPUP + LAYERED(255) + TOOLWINDOW + NOACTIVATE + HWND_BOTTOM**（Progman/WorkerW 子窗口在 24H2 收不到点击，是死路）；工具箱自定义窗口类 + 两次沉底；几何只用 GetSystemMetrics。开工顺序：装 BepInEx → 建插件工程搬代码 → 干掉 Win+D 空桌面（同进程 subclass / 协程自愈）→ 打包发 GitHub。
