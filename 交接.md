# ChillDesktop 交接文档

> 最后更新：2026-09-10 下午
> 状态：**插件版已完成并实机验证通过。原第一优先级缺陷「Win+D 显示空桌面」已修复。**
> 本机已装 BepInEx 5.4.23.5 用于实测（`tools\Uninstall-BepInEx.ps1` 可完全卸载）。
> 发布包已能一键生成（`tools\Pack-Release.ps1`）。
> 下一步：录 GIF / 写 Release 说明 / 找人实机复测（干净环境安装-卸载）→ 发 GitHub Release。

---

## 1. 这个项目是什么

把 Steam Unity 游戏 **《Chill with You Lo-Fi Story》** 变成一个**可交互的动态桌面壁纸**：

- 游戏全屏铺在桌面背景层，女孩持续动画、lo-fi BGM 持续播放；
- IDE / 浏览器 / 资源管理器等办公窗口正常浮在她上面，缝隙处能看到她；
- 游戏内原生 UI（笔记 / 待办 / 习惯 / 日历 / 隐藏UI）**在壁纸状态下可正常点击**；
- 游戏右侧「笔记」按钮旁边，额外伪装了一个等大的灰色圆圈按钮 **「工具箱」**，点击打开桌面上真实的 `chillgirl` 文件夹；
- 点击壁纸层不抢办公窗口焦点、不把游戏抬到办公窗口之上、**Win+D 不会让游戏消失**。

### 产品形态（已定，已实现）

**BepInEx 5 插件**（C#，net46，x64/Mono），拖进游戏根目录即生效。
交付形态是**方案 A**：一个压缩包内含 BepInEx 全家桶 + 插件 dll + README，
用户整个拖进游戏根目录即完成安装。

- 包内**不含**任何游戏文件。
- BepInEx 是 LGPL-2.1，随包分发没问题（见 `THIRD-PARTY-NOTICES.md`）。
- 已写明「非官方 mod、与开发商无关、自负风险」。

---

## 2. 关键事实与踩坑（全部实机确认）

### 2.1 游戏侧事实

- Unity **2022.3.62f2 Mono、x64**、D3D11 flip-model；窗口类 `UnityWndClass`，标题 `Chill With You`。
- 游戏进程是 **DPI-aware**（实测 `Display.main.systemWidth` = `GetSystemMetrics` = 2560×1600）。
- 失焦不暂停，失焦自动降到约 24fps。
- **游戏启动时会检查 `RestartAppIfNecessary`**：直接双击 exe 启动会被判定「不是 Steam 启动」，
  抛 `RestartAppIfNecessaryFromAPIException` 并让 Steam 重启它（本机 Steam 处于 `-silent` 状态时
  这一步会卡住，表现为「游戏一闪就退、Steam 也拉不起来」）。
  **开发调试两种可行做法**：① 从 Steam 正常启动；② 游戏根目录放一个内容为 `3548580` 的
  `steam_appid.txt` 再直接启动 exe（本机当前就是这种，**发布包绝对不能带这个文件**）。

### 2.2 ⚠️ 最大的坑：BepInEx 给插件挂的 GameObject 会被游戏销毁

实测日志：

```
13:57:47.335  启动 Bootstrap 协程……
13:57:47.342  OnEnable 被调用
13:57:47.617  OnDisable 被调用      ← 275ms 后
13:57:47.617  OnDestroy 被调用
[诊断] Update 次数=0 …（此后永远为 0）
```

游戏自己的场景加载会把 BepInEx 挂在插件上的那对象干掉，
于是插件的 **`Update()` 和 `StartCoroutine()` 全部静默失效**——不报错、不崩溃，就是不动。

**解法（已实施）**：插件自己 `new GameObject("ChillDesktop_Runner")` +
`DontDestroyOnLoad` + `hideFlags = HideFlags.HideAndDontSave`，把主循环挂在自建对象上。
实测自建对象的 `Update` 被正常调用（每秒几百次），能活过场景加载。

> 排查手法值得复用：起一个 `System.Threading.Timer` 后台线程打印状态，
> 它不受 Unity 调度影响，能一刀切开「协程没被 pump」和「函数卡死」两种可能。

### 2.3 ⚠️ 等待必须用 `WaitForSecondsRealtime`

加载阶段 `Time.timeScale` 可能是 0。用缩放时间的 `WaitForSeconds`，协程 yield 之后
**永远醒不过来**（第一版就踩了）。所有等待一律 `WaitForSecondsRealtime`。

### 2.4 ✅ Win+D 空桌面：已修复（原第一优先级缺口）

**现象（旧版外部启动器）**：壁纸状态下按 Win+D，游戏被藏走，露出空桌面。

**实测结论**：插件版铺底后按 Win+D，游戏窗口**全程 `IsIconic=False`、`visible=True`、矩形不变**，
`WindowGuard` 的钩子甚至连 `SC_MINIMIZE` 都没收到——说明 `WS_EX_TOOLWINDOW` + 去掉
`WS_MINIMIZEBOX` 之后，MinimizeAll 本来就跳过它。原先的缺口大概率来自原型跨进程改造窗口时的
某种状态差异，而不是「NOACTIVATE 一定被最小化」。

**客观验证方式（可复用）**：`poc\verify_wallpaper.ps1` 把「屏幕截图」和
「游戏窗口自己的 `PrintWindow(PW_RENDERFULLCONTENT)` 抓帧」各缩到 64×40 逐像素比对：

| 场景 | 全屏 MAE | 结论 |
|---|---|---|
| 不按键（Edge 盖在上面） | 109.34 | 看不到游戏（正常） |
| 按 Win+D（隐藏桌面图标） | **0.08** | **屏幕上就是游戏画面 → 壁纸可见 ✔** |
| 按 Win+D（**不**隐藏桌面图标） | 82.78 | 桌面层盖到游戏上面 ✘ |

> **重要推论**：`HideDesktopIcons` **必须保持 true**。桌面图标层（SHELLDLL_DefView）可见时，
> Win+D 会把桌面层抬到我们沉底的游戏窗口之上，壁纸就没了。默认值已经是 true，别乱改。

**仍然保留的三重防御**（万一在别的机器/系统版本上 MinimizeAll 会动手）：

1. `WindowGuard`：同进程 `SetWindowLongPtr(GWLP_WNDPROC)` 给游戏窗口过程装钩子，
   观察 `WM_SYSCOMMAND / WM_SIZE / WM_SHOWWINDOW`，并直接吃掉 `SC_MINIMIZE`
   （插件在游戏进程内才做得到，外部启动器做不了）；
2. `FastPollSeconds`（默认 0.25s）：发现 `IsIconic` / 不可见 / 被重新 SetParent 走，
   立刻 `SW_RESTORE` + 重新铺底 + 维持「工具箱→游戏」两次沉底；
3. `UseOwnerWindow`（默认 false）：给游戏窗口挂隐藏属主窗，MinimizeAll 会跳过有属主的窗口。
   前两个不够用时再开。

### 2.5 废案：挂进 Progman / WorkerW 当子窗口（别再走）

24H2/25H2 上 `SetParent(游戏, WorkerW/Progman)` 后，WS_CHILD 子窗口**几何上能命中测试，
但系统不投递真实鼠标点击**（GDI 测试窗和 Unity 游戏都一样）→ 悬停有反应、所有原生按钮全失效。
PostMessage 伪造点击也无效。**这不是 bug，是系统限制**，任何「转发子窗口点击」的尝试都别再走。

### 2.6 最终成立的窗口配方

```
样式   : WS_POPUP（无边框、无标题栏、不可调大小）+ WS_VISIBLE + WS_CLIPSIBLINGS
扩展样 : WS_EX_LAYERED (alpha=255) + WS_EX_TOOLWINDOW + WS_EX_NOACTIVATE
位置   : 铺满主屏 (0,0,ScreenW,ScreenH)，SetWindowPos(HWND_BOTTOM, SWP_NOACTIVATE)
```

三件事兼得：真实点击进游戏、点击不抢焦点也不抬升、窗口沉在所有办公窗口之下。

- **工具箱 Z 序**：`SetWindowPos(A, B)` 是把 A 放到 B **下方**。想「工具箱贴在游戏上方」要
  **两次沉底**：先把工具箱 `HWND_BOTTOM`，再把游戏 `HWND_BOTTOM`，后沉的更靠底 ⇒
  `办公窗口 … 工具箱 → 游戏 → Progman`。
- **工具箱窗口类必须自定义注册**。预定义 `"Static"` 的 `WM_NCHITTEST` 默认返回
  `HTTRANSPARENT`（点穿），顶级窗也收不到点击；自己 `RegisterClassW`（默认 HTCLIENT）
  才能在 `WM_NCHITTEST` 里按圆形/标签形状返回 `HTCLIENT` / `HTTRANSPARENT` 做逐形状穿透。
- **几何只用屏幕物理像素**：插件内用 `Display.main.systemWidth/Height`（Unity 侧真实物理像素），
  退回 `GetSystemMetrics`。跨进程对游戏窗口调 `GetWindowRect/GetClientRect` 会拿到 DPI 虚拟化坐标。
- **绘制**：`UpdateLayeredWindow` + top-down 32bpp 预乘 alpha DIB；透明层上文字用**灰度 AntiAlias**，
  ClearType 会留 RGB 彩边。

### 2.7 桌面图标隐藏的两面性

- 必须隐藏（见 2.4 的对照实验）。
- **风险**：游戏被**强杀**（任务管理器结束进程）时插件来不及还原，桌面图标会一直保持隐藏。
  - 插件内已有两道保险：`AppDomain.ProcessExit`、`OnDestroy`/`OnApplicationQuit`，
    以及心跳连续 3 次找不到游戏窗口时提前还原图标；
  - 强杀场景无法覆盖，**兜底脚本**：`tools\Restore-DesktopIcons.ps1`。
  - README 的「已知问题」里已经写明。

---

## 3. 代码结构

```
ChillDesktop\                     ← 仓库根
├─ ChillDesktop.Plugin\           ★ 正式产品：BepInEx 5 插件（net46）
│   ├─ Plugin.cs                  [BepInPlugin] 入口：读配置、进程名校验、挂 Runner
│   ├─ Runner.cs                  ★ 主循环（挂自建 GameObject）：等窗口→铺底→快速自愈→心跳→热键→退出清理
│   ├─ PluginConfig.cs            cfg 绑定（General / WinD / Toolbox / Layout 四组）
│   ├─ Log.cs                     写 BepInEx 控制台 + BepInEx\chilldesktop.log
│   ├─ Desktop\DesktopLayer.cs    找本进程窗口、EmbedGame/Detach、图标层、心跳、屏幕尺寸
│   ├─ Desktop\WindowGuard.cs     Win+D 防御：窗口过程钩子 + 可选隐藏属主窗
│   ├─ Widget\FolderWidget.cs     工具箱圆圈（去 WinForms：静态委托当 lpfnWndProc）
│   ├─ Widget\LayeredRenderer.cs  UpdateLayeredWindow 呈现
│   └─ Native\User32.cs / Gdi32.cs  P/Invoke
├─ ChillDesktop\                  早期外部启动器原型（.NET 8 WinForms），保留作参考
├─ poc\                           验证/取证脚本
│   ├─ verify_wallpaper.ps1       ★ 壁纸可见性客观比对（MAE），支持 -PressWinD
│   ├─ wind_probe.ps1             ★ Win+D 现场取证：按 Win+D 后每 100ms 采样窗口状态
│   └─ …（历史实验脚本 + 截图）
├─ tools\
│   ├─ Install-BepInEx.ps1 / Uninstall-BepInEx.ps1
│   ├─ Pack-Release.ps1           ← 打发布包（方案 A）
│   └─ Restore-DesktopIcons.ps1   ← 强杀后恢复桌面图标的兜底脚本
├─ lib\bepinex\BepInEx.dll        编译期引用（BepInEx 5.4.23.5 原样，未改）
└─ README.md / LICENSE / THIRD-PARTY-NOTICES.md
```

### 构建

```
dotnet build ChillDesktop.Plugin\ChillDesktop.Plugin.csproj            # Debug，自动部署到游戏 plugins\
dotnet build -c Release ChillDesktop.Plugin\ChillDesktop.Plugin.csproj
tools\Pack-Release.ps1                                                 # → dist\ChillDesktop-0.1.0-BepInEx5-x64.zip
```

- 编译期需要游戏自带的 `UnityEngine*.dll`。**Unity 2022.3 的托管程序集是按 .NET Standard 2.1
  编译的，从 net46 工程引用会报 `CS0012`（要求 netstandard 2.1 引用）**。
  解法：把游戏 `Managed\netstandard.dll`（纯类型转发 facade）也加进 `<Reference>`，
  见 csproj 里的注释。发布包不带任何 Unity 程序集（`Private=false`）。
- 游戏不在默认 Steam 路径时：`-p:GameDir="..."` 或环境变量 `CHILL_GAME_DIR`。
- 构建后自动拷贝到 `<游戏目录>\BepInEx\plugins\`（`-p:DeployToGame=false` 可关）。

---

## 4. 实机验证记录（本机 Win11 2560×1600 / 150%）

全链路已通过：

1. BepInEx 5.4.23.5 正常加载，Chainloader 启动完成，插件被识别为 `ChillDesktop 0.1.0`；
2. 找到游戏窗口 → 记录原始样式 → 铺底 → 装窗口钩子 → 隐藏桌面图标 → 创建工具箱，
   日志逐条确认；工具箱窗口 `Ex=0x08080080`（LAYERED+TOOLWINDOW+NOACTIVATE），
   位置换算与设计值一致（圆外接矩形 2162,613 214×108）；
3. **Win+D**：`iconic=False / visible=True / 矩形不变`；MAE 0.08 → 壁纸完整可见 ✔
4. **优雅退出**（对游戏窗口发 `WM_CLOSE`）：`插件卸载（OnApplicationQuit）` →
   `窗口钩子已卸载` → `游戏窗口已还原为普通窗口` → `桌面图标已恢复`，全部执行 ✔
5. 强杀场景：图标会保持隐藏，`tools\Restore-DesktopIcons.ps1` 可恢复 ✔（脚本本身已实测）

**尚未做**：干净环境（未装过 BepInEx）的安装/卸载复测；别的分辨率与多显示器；游戏大版本更新后的复测。

---

## 5. 下次要做的事（按优先级）

1. **录 GIF / 截图**：壁纸效果、原生 UI 可点、工具箱、Win+D 前后对比 —— README 和 Release 都要用。
2. **发 GitHub Release**：上传 `dist\ChillDesktop-0.1.0-BepInEx5-x64.zip`，
   说明写清「安装三步 / 卸载五删 / 非官方 mod 免责」。
3. **干净环境复测**：找一台没装过 BepInEx 的机器（或先跑 `Uninstall-BepInEx.ps1` 清干净），
   验证「拖进去 → 从 Steam 启动」的完整路径，以及卸载后游戏是否完全正常。
4. **工具箱 IMGUI 版（可选但推荐）**：用 Unity `OnGUI` 直接在游戏画面里画圆，点击走 Unity 事件，
   位置/DPI/Z 序问题全部消失，观感也更贴合游戏原生 UI。现在的外挂窗留作 fallback。
   - 需要给 csproj 加 `UnityEngine.IMGUIModule.dll` 引用。
5. **多显示器 / 分辨率变化**：目前只处理主屏；分辨率变化靠 3 秒心跳重排。
6. **可选增强**：失焦 24fps 观感（插件内可直接调 `Application.targetFrameRate`）；
   按钮位置常量已经全部做成 cfg（`[Layout]`）。

---

## 6. 一句话给下一个我

> 产品是 **BepInEx 5 插件**，窗口配方是 **顶级 WS_POPUP + LAYERED(255) + TOOLWINDOW + NOACTIVATE + HWND_BOTTOM**
> （Progman/WorkerW 子窗口在 24H2 收不到点击，是死路）；**主循环必须挂在插件自建
> `DontDestroyOnLoad` 的 GameObject 上**（BepInEx 那个会被游戏销毁），等待一律用
> `WaitForSecondsRealtime`；**`HideDesktopIcons` 必须为 true**，否则 Win+D 时桌面层会盖住壁纸。
> 验证壁纸是否真的可见，用 `poc\verify_wallpaper.ps1` 的 MAE 比对（<12 即可见）。
> 本机调试用 `steam_appid.txt` 直接启动 exe，**发布包绝不能带这个文件**。
